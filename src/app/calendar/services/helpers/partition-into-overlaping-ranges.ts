const dateSort = (a: CalendarEvent, b: Date): number => {
  //   if (a.start < b.start) return -1;
  //   if (a.start > b.start) return 1;
  //   return 0;
};

export const partitionIntoOverlappingRanges = array => {};
